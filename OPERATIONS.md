# サイト更新手順

## 更新前の確認

1. 公演日、会場、出演者、料金、チケットURLを主催者の一次情報と照合します。
2. プロフィール、連絡先、画像の掲載許可を確認します。
3. 未確認の情報は公開状態を `draft` のままにします。

## CMSでの更新

1. `https://kawatake0910.github.io/my-vite-site/admin/` を開いてログインします。
2. 更新対象（ニュース、公演情報、プロフィール、連絡先・SNS）を選びます。
3. 必須項目を入力し、下書きとして保存します。
4. 変更提案が作成されたら、GitHubのPull Requestで内容と自動ビルド結果を確認します。
5. 承認後に `master` へマージします。GitHub Actionsが自動でGitHub Pagesへ公開します。
6. 公開後、PCとスマートフォンで表示、リンク、画像を確認します。

## 公演情報のルール

- `draft` は公開されません。
- `published` は掲載終了日まで公開されます。
- 中止・延期では `cancelled` に変更し、内容と公式案内URLを更新します。
- 終了後の実績は `archived` に変更します。
- 日時、会場、URLはAIの生成結果だけで決めず、必ず一次情報で確認します。

## 定期点検

- 毎週: ニュース、公演情報、チケットURL
- 毎月: 連絡先、SNSリンク、スマートフォン表示
- 四半期ごと: プロフィール、掲載画像の権利、不要になった情報

## 緊急時

誤公開時は、該当するPull RequestまたはコミットをGitHubでrevertし、公開サイトが戻ったことを確認します。秘密情報、パスワード、APIキーはCMSの入力欄やリポジトリに保存しません。
